import React from 'react';
import ReactDOM from 'react-dom/client';
import Mathiko from './mathiko.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Mathiko />
  </React.StrictMode>
);
